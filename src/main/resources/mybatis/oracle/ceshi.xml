<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace = "com.liufan.mutildatasource.dao.oracle.OracleDao">

    <select id="selectOracle" resultType="map">
        SELECT bmid FROM BMDOC where bmid='DBM00000009'
    </select>
</mapper>
